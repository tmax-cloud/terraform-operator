
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: tfapplyclaims.claim.tmax.io
spec:
  group: claim.tmax.io
  names:
    kind: TFApplyClaim
    listKind: TFApplyClaimList
    plural: tfapplyclaims
    singular: tfapplyclaim
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: TFApplyClaim is the Schema for the tfapplyclaims API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: TFApplyClaimSpec defines the desired state of TFApplyClaim
          properties:
            branch:
              type: string
            destroy:
              description: Size    int32  `json:"size,omitempty"` Plan    bool   `json:"plan,omitempty"`
                Apply   bool   `json:"apply,omitempty"`
              type: boolean
            secret:
              description: Email   string `json:"email,omitempty"` ID      string
                `json:"id,omitempty"` PW      string `json:"pw,omitempty"`
              type: string
            type:
              description: Foo is an example field of TFApplyClaim. Edit TFApplyClaim_types.go
                to remove/update
              type: string
            url:
              type: string
            version:
              type: string
          type: object
        status:
          description: TFApplyClaimStatus defines the observed state of TFApplyClaim
          properties:
            action:
              type: string
            apply:
              type: string
            commit:
              type: string
            destroy:
              type: string
            log:
              type: string
            nodes:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file'
              items:
                type: string
              type: array
            phase:
              type: string
            plans:
              items:
                properties:
                  lastexectiontime:
                    type: string
                  log:
                    type: string
                type: object
              type: array
            resource:
              properties:
                added:
                  type: integer
                deleted:
                  type: integer
                updated:
                  type: integer
              type: object
            state:
              type: string
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
